package headers

// Host specifies the domain name of the server (for virtual hosting),
// and (optionally) the TCP port number on which the server is listenting.
const Host = "Host"

// Referrer is the address of the previous web page from which a link to the currently requested page was followed.
const Referrer = "Referer"

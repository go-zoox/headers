package headers

// Date contains the date and time at which the message was originated.
const Date = "Date"

// Link is the Link entity-header field provides a means for serializing one or more links in HTTP headers. It is semantically equivalent to the HTML <link> element.
const Link = "Link"
